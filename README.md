# FalconHostSearch
A CMD tool to search CrowdStrike Falcon for hosts by IP, hostname, OS/type, or login user, and optionally pull each host's vulnerabilities and open ports from Tenable, its installed applications, and its recent detections. It can also query CrowdStrike threat intelligence (actors and reports) standalone, with no host search at all.
